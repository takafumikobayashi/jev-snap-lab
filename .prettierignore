# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# 設計ドキュメント（手で整形した日本語散文。Prettier の再整形で
# 表の桁ずれと不要な差分が出るため対象外にする）
/docs/

# 生成するデータセット。scripts/build-*.mjs が生成し、整形もそちらが担う。
# Prettier を通すと、生成と整形で形式が食い違い、差分が往復する。
/data/
